// ============================================================
// 매장 설정 — 여기 값만 바꾸면 사이트 전체에 반영됩니다.
// ============================================================

const CONFIG = {
  // 매장 이름 (사이트 로고/타이틀에 사용)
  storeName: "폰마트",

  // 상담 전화번호 (예: "010-1234-5678")
  phone: "010-0000-0000",

  // 카카오톡 채널 URL (예: "https://pf.kakao.com/_abcdef")
  // 비워두면 카톡 버튼이 숨겨집니다.
  kakaoUrl: "",

  // 영업시간 안내 문구
  hours: "매일 09:00 ~ 21:00 (연중무휴)",

  // ----------------------------------------------------------
  // Supabase 연동 (선택사항)
  // 설정하면 상담신청이 데이터베이스에 자동 저장됩니다.
  // 비워두면 상담신청 시 문자(SMS) 앱으로 연결됩니다.
  //
  // 연동 방법: README.md 의 "상담신청 DB 연동" 참고
  // ----------------------------------------------------------
  supabaseUrl: "",        // 예: "https://xxxx.supabase.co"
  supabaseAnonKey: ""     // publishable(anon) 키
};
